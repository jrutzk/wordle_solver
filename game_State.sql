CREATE OR REPLACE VIEW WORDLE.GAME_STATE_VIEW AS
SELECT
	'green',
	POSITION::CHAR,
	LETTER
FROM
	WORDLE.GREEN
	where letter != '_'
UNION ALL
SELECT
	'yellow',
	POSITION::CHAR,
	LETTER
FROM
	WORDLE.YELLOW
UNION ALL
SELECT
distinct
	'grey',
	'-',
	LETTER
FROM
	WORDLE.GREY;
