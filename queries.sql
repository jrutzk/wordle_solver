SELECT
	POS,
	LETTER,
	COUNT(*)
FROM
	WORDLE.GUESSES_UNPIVOT_VIEW
GROUP BY
	POS,
	LETTER
ORDER BY
	COUNT(*) DESC;

SELECT
	--POS,
	LETTER,
	COUNT(*)
FROM
	WORDLE.GUESSES_UNPIVOT_VIEW
GROUP BY
	--POS,
	LETTER
ORDER BY
	COUNT(*) DESC;

-- CRANE STOUP DIMLY
